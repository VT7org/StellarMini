version: "3"
services:
  bot:
    build: .
    command: python bot/main.py
    env_file: .env
  backend:
    build: .
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file: .env